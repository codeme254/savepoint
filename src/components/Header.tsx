export default function Header() {
  return (
    <header className="header">
      <h1 className="logo-text">SavePoint</h1>
      <nav className="navigation">
        <button className="logout-btn">Logout</button>
      </nav>
    </header>
  );
}
